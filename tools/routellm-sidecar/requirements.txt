# Verified 2026-09-25 on Linux x86_64, Python 3.12.14, CPU-only torch.
# Install:  uv venv --python 3.12 .venv
#           VIRTUAL_ENV=.venv uv pip install --index-strategy unsafe-best-match \
#             --extra-index-url https://download.pytorch.org/whl/cpu -r requirements.txt
# (For CUDA use the matching PyTorch index instead of /whl/cpu.)
routellm==0.2.0
torch==2.14.0
transformers==5.17.0
tokenizers==0.23.2
huggingface-hub==1.33.0
numpy==2.5.3
safetensors==0.8.0
openai==2.54.0
