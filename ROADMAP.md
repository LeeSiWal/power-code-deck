# Roadmap

현재 버전: **v0.5.0** (할일 체크리스트 · 알림 센터 · 동반 셸). 릴리스 내역은 [CHANGELOG.md](CHANGELOG.md) 참고.

---

## 다음 (v0.6.0 방향)

v0.5.0에서 할일 체크리스트·알림 센터·동반 셸이 나갔고, 전체 허용 모드 불안정과
세션 재진입 시 활동 표시 누락도 종료됐습니다. **남은 것은 셋** — 선행 작업이 큰 것 하나,
스펙이 필요한 것 하나, 그리고 의도적으로 미룬 것 하나. 열려 있는 버그는 없습니다.

### 1. 알림 → 해당 작업으로 바로 이동 — 🟡 서버만 완료

알림 센터와 분리한 이유는 **선행 작업이 크기 때문**입니다. 그중 절반이 끝났습니다.

**완료** — 알림에 참조 필드. `notifications`에 `ref_type`/`ref_id` 컬럼과
`CreateWithRef()`가 들어갔고, `pushNotify`가 승인은 `requestId`를, 나머지는 `chat/latest`를
함께 기록합니다.

**남은 장벽** — 클라이언트에 **앵커가 없습니다.** 네이티브 채팅 아이템의 id는
`foldEvents()`가 매번 새로 만드는 합성값(`s0`, `t0`)이라 스크롤할 대상 자체가 존재하지
않습니다. 원본 이벤트에 안정적인 id를 부여하고 그것을 fold 결과까지 보존하는 것이 실제
작업입니다.

지금은 알림을 탭하면 `/agents/{id}`(세션)까지 갑니다 — 대부분의 경우 이것으로 충분한지,
정말 특정 지점까지 필요한지 실사용으로 판단한 뒤 착수하는 편이 낫습니다.

### 2. 승인 정책 — "앞으로도 허용"

지금은 모든 승인이 일회성입니다. 같은 도구·명령을 매번 다시 묻습니다.
허용 목록(allowlist)이나 "이 결정을 기억" 개념이 전혀 없습니다.

**설계 여지가 커서 스펙이 필요합니다.** 무엇을 기억할지가 보안 성격을 바꿉니다 —
도구 단위인지, 도구+인자 패턴인지, 세션 한정인지 영구인지, 프로젝트 범위인지.

참고: 서버 프로토콜은 이미 `updatedInput`(입력을 고쳐서 승인)을 지원하지만 UI가
노출하지 않습니다. 같이 다룰지 검토합니다.

### 3. 관제실 활동 표시 — ⬜ 의도적 보류

원래 목적이 "벽에서 이 세션이 뭘 하는지 안다"였는데,
타일의 할일 줄이 이미 그것을 semantic하게 전달합니다(`lastTool`은 `Edit` 같은 도구 이름이라
의미를 전달하지 못했습니다). 활동 스트림을 요약에 싣는 서버 작업은 비용이 있으므로,
**실사용에서 여전히 부족한지 확인한 뒤** 착수합니다 — 아니면 필요 없는 것을 만들게 됩니다.

---

## 완료됨

### v0.5.0 — 할일 체크리스트 · 알림 센터 · 동반 셸
- 할일 체크리스트 3표면(관제실 타일 · 세션 스트립 · 채팅 카드)
- 알림 센터 + 진입점 네 곳, 앱을 보고 있는 기기에는 푸시 억제
- 동반 셸, 셸 암호 입력 수정, 전체 허용 오탐 배너 제거
- 자세한 내용은 [CHANGELOG.md](CHANGELOG.md) 참고

#### 전체 허용 모드 안정화 (v0.5.0에서 종료)

보고된 증상 두 가지 — **전체 허용인데 멈춤**, **모드 전환 직후 동작이 이상** — 은
하나의 원인에서 나왔고 수정됐습니다.

**근본 원인: `restart()`의 권한 정책 유실 구간.**
`restart()`가 `sessions` 맵에서 세션을 지운 뒤 `startSession`이 다시 넣기까지 공백이
생기는데, `autoDecision`이 바로 그 맵에서 모드를 읽었습니다. 그래서 이 창에 도착한
권한 요청은 전체 허용이든 자동이든 **정책을 통째로 건너뛰고 사람에게** 갔습니다.
전체 허용 사용자는 승인 카드를 보고 있지 않고, 브리지의 HTTP 호출에는 타임아웃이
없으므로(`mcp_approve.go`, `Timeout: 0`) 도구 호출이 무기한 매달렸습니다 — 이것이
"멈춤"의 정체입니다.

수정: 모드·cwd를 세션 객체와 생애주기가 다른 `policies` 맵에 보관합니다. 모드는
드라이버 프로세스가 아니라 **에이전트(사용자 선택)**에 귀속되며, 재시작은 드라이버를
갈아치울 뿐 그 선택을 바꾸지 않습니다. 테스트 3개가 고정합니다 —
`TestBypassPolicySurvivesRestartWindow` / `TestAutoPolicySurvivesRestartWindow`,
그리고 게이트가 넓어지지 않았음을 지키는 `TestUnknownSessionStillAsksAHuman`.

**곁가지로 해소된 것: 브리지 관련 주석 모순.**
`claude_driver.go`가 맞았습니다 — `bypassPermissions`에서는 `--mcp-config`와
`--permission-prompt-tool`을 **의도적으로 넘기지 않습니다**(이 CLI는 플래그와 프롬프트
툴이 동시에 있으면 오히려 DENY). `permission_policy.go`·`native_service.go`에 있던
"플래그에도 CLI가 우리 승인 툴을 계속 호출한다"는 서술이 낡은 것이었고 바로잡았습니다.
따라서 `autoDecide`의 bypass 분기는 주 경로가 아니라 **restart 구간용 backstop**입니다.

같은 사실이 UI 오탐도 설명합니다: 전체 허용에서 브리지가 없는 것은 정상인데
"승인 브리지가 연결되지 않았습니다 — 전부 자동 거부됩니다" 배너가 떴습니다. 그 모드에서만
숨기도록 고쳤습니다(다른 모드에서는 실제로 전부 거부되므로 경고를 유지).

#### 관찰된 적 없음 — 만들지 않은 것

재시작 시 화면의 승인 카드가 남을 **이론적** 창이 있습니다. 서버 쪽은 스스로 정리됩니다 —
옛 CLI가 죽으면 그 브리지의 HTTP 요청이 끊기고, `Ask`의 `case <-cancel: b.forget(req.ID)`가
pending을 지웁니다. 다만 `approval:resolved`는 **사람이 결정했을 때만** 방송되고
(`ws/hub.go`), `EventNativeSetMode` 핸들러는 성공 시 아무 상태도 밀지 않으므로, 클라이언트
카드는 다음 `native:state`가 올 때까지 남을 수 있습니다.

**실제로 관찰된 적이 없습니다.** 승인이 떠 있는 상태에서 모드를 바꾸는 상황 자체가 드물고
(보통 승인을 처리한 뒤 바꿉니다), 잔상일 뿐 실질 피해가 없습니다. 고친다면
`SetMode`/`SetModel` 성공 시에도 `native:state`를 한 번 미는 3줄이지만, **재현되기 전에는
만들지 않습니다.** 재현되면 이 문단이 출발점입니다.

> 초기 진단에서 이것을 "`current`가 false라 `CancelSession`이 안 돌아 카드가 남는다"고
> 적었으나 틀렸습니다. 토큰 회수에는 해당하지만 pending 정리는 위의 다른 경로로 이미
> 이뤄지고 있었습니다.

#### 세션 재진입 시 활동 표시 (v0.5.0에서 종료)

재진입 시 활동 패널이 비어 보이던 문제. `native:open`과 `shell:attach`가 붙는 시점에
서버가 최신 활동 스냅샷을 직접 밀어주도록 하며 해결됐습니다(`hub.sendActivitySnapshot`,
`ActivityManager.Latest`). 그 전에는 다음 폴링 틱까지 기다려야 했습니다.

> 조사 중 확인된 잔여 비대칭: `terminal:attach`에는 이 스냅샷 푸시가 없습니다. 다만
> `emitSnapshot`이 800ms마다 계속 내보내므로 공백은 최대 한 틱이고, 터미널 트랙(셸·커스텀
> 명령)에는 애초에 표시할 Claude 활동이 거의 없습니다. **의도적으로 맞추지 않았습니다** —
> 없애는 이득보다 코드가 느는 비용이 큽니다. 터미널 트랙에서 활동 표시가 실제로 필요해지면
> 그때 같은 한 줄을 추가하면 됩니다.

#### 알림 센터 (v0.5.0에서 완료)

배지와 토스트만 있어 토스트를 놓치면 이벤트가 사라졌습니다. 서버는 계속 기록하고 있었으므로
이 작업은 그것을 보여주는 것이었습니다.

- `/notifications` — 읽음/안읽음 구분, 종류별 좌측 색, 세션 이름, 시각. 탭하면 그 세션으로 이동
- 지우기 — 서버 clear + 로컬 스토어를 함께 비웁니다. 배지는 서버의 unread 행이 아니라
  라이브 WS 이벤트 맵을 세므로, 한쪽만 비우면 목록은 비었는데 배지가 남아
  "지우기가 안 먹었다"로 읽힙니다
- 진입점 네 곳 — 모바일 하단 탭(Alerts) · 모바일 세션 헤더 · 데스크톱 관제실 헤더 ·
  데스크톱 세션 헤더. 세션 화면에는 BottomNav가 없어서(집중 화면이라 의도된 것) 세션에
  들어와 있는 동안 알림으로 갈 경로가 아예 없었습니다
- 라벨·색은 `components/notification/reasons.ts`로 토스터와 공유합니다 — 같은 이벤트가
  두 화면에서 다른 이름으로 보이면 서로 다른 사건처럼 읽힙니다

모바일 세션 헤더의 자리는 핸드오프 버튼에서 가져왔습니다. 핸드오프는 데스크톱 세션을 QR로
폰에 넘기는 기능이라 폰에서는 자기 자신에게 넘기는 셈이 됩니다(데스크톱 헤더에는 그대로 유지).

### v0.4.0 — 통합 관제실
- `/dashboard`를 `/control`에 흡수, 타일에 git 메타와 삭제 액션
- 뒤로 가기를 히스토리 기반(`navigate(-1)`)에서 계층 기반으로 전환
- `AskUserQuestion`에 승인 카드가 같이 뜨던 버그 수정
- 자세한 내용은 [CHANGELOG.md](CHANGELOG.md) 참고

### v0.3.0 — Control Room
- 멀티 세션 관제실, 프로젝트별 그룹핑, 3상태 애니메이션
- Attention 레일, 전역 승인 큐, 세션 정지/재시작
- 플러그인 관리, UI 크기 조절

### 방향 전환 (2026-07-17) — 네이티브 UI로

**결정: Antigravity(agy) 프리셋을 뺀다. Claude와 Codex에 집중한다.**

조사 결과 agy는 대화형 헤드리스 인터페이스가 없다 — 이벤트 스트림
(`--output-format stream-json`, 문서에 없는 숨은 플래그)은 있지만 **단방향**이라
승인도 후속 입력도 중단도 불가능하고, CLI 자신이 *"permission that headless mode
cannot prompt for"* 라고 말한다. 게다가 승인에 막혀 아무 일도 안 한 실행을
`status: SUCCESS`로 보고한다. ACP 지원 요청(이슈 #31)은 메인테이너 무응답 상태다.

반면 Claude와 Codex는 **양방향 구조화 스트림**이 있다:
- Claude — `-p --input-format stream-json --output-format stream-json` + `canUseTool`
  (허용/입력 수정 후 허용/규칙 저장/거부+메시지, `AskUserQuestion`, **`defer`**)
- Codex — `codex app-server` (JSON-RPC 2.0 / stdio, 공식 VS Code 확장이 쓰는 것)
  + `serverRequest` 승인

그래서 터미널로 TUI를 흉내내는 대신 **구조화 이벤트를 네이티브 UI로 그린다.**
그러면 우리 갭 목록의 대부분(폭 테이블, DEC 모드, 리플로우, 쿼리 응답, replay
직렬화, 마우스, 커서)이 고쳐지는 게 아니라 **존재하지 않게 된다.** 무엇보다
채팅 UI는 폰 너비로 리플로우된다 — TUI는 고정 격자라 그게 안 된다.

자세한 근거와 설계: [docs/terminal-stability-and-native-ui.md](docs/terminal-stability-and-native-ui.md)

- [x] Claude 네이티브 드라이버 (stream-json + 승인)
- [x] 네이티브 채팅 UI (메시지/도구 호출/승인 버튼)
- [x] Codex 드라이버 (`app-server`, 생성 JSON Schema 기준)
- [ ] grace-time 재접속 (60초 → 6초, 두 트랙 공용)

#### 터미널 트랙 — 셸용으로 동결
셸·vim·임의 CLI를 위해 남기지만, TUI 앱을 호스팅하기 위한 투자는 하지 않는다.
따라서 **서버 이뮬레이터(node 사이드카)는 불필요**하고 `pcd` 단일 바이너리를
유지한다. 마우스 전체 지원·커서 상태·스크롤백 DOM 재작성도 하지 않는다 —
전부 TUI 앱을 위한 비용이었다.

이미 들어간 것(유지):
- **DECRQM 쿼리 응답** — xterm.js와 동일한 올바른 동작. 어떤 TUI에도 필요하다.
- **ACK 기반 flow control** — 대량 출력 백프레셔 (VS Code와 같은 설계)
- **유니코드 V11** — 이모지 폭 1셀→2셀 (한글은 V6에서도 2셀이라 무관했다)
- **쓰기 권한 게이트** — attach된 뷰어만 세션에 쓸 수 있다
- **빈 화면 계측** — 렌더 정지의 원인(런 수/시간/DOM vs 버퍼)을 화면에 표시

### Session Handoff (Continue on Mobile) — 구현 완료
- 데스크톱 세션을 모바일 / iPad로 이어받는 핸드오프 기능. **구현 완료.**
- QR 코드 + **일회용 토큰**(SHA-256 해시 저장, 기본 10분 TTL, 단일 사용, 세션 바인딩)으로 인계.
- Public URL / LAN 핸드오프 지원, 바인드 호스트 설정 가능(기본 `127.0.0.1`).
- 도착 시 모바일 Prompt Bar 자동 확장. 자세한 내용은 [README](README.md#session-handoff) · [CHANGELOG.md](CHANGELOG.md) 참고.

### v0.2.0 — PowerCodeDeck Renewal
- AgentDeck → PowerCodeDeck 리브랜딩, 버전 관리 도입
- 선택형 인증(none/PIN/password), 기본값 인증 없음
- `POWERCODEDECK_*` 환경변수 + `AGENTDECK_*` 하위 호환
- 자세한 내용은 [CHANGELOG.md](CHANGELOG.md) 참고
