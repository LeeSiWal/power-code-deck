# Binaries
pcd
agentdeck

# Secrets / local config
.env

# SQLite data (+ WAL/SHM)
powercodedeck.db*
agentdeck.db*

# Deps / build output
node_modules/
client/dist/
server/static/

# OS / logs
.DS_Store
*.log

# Newton run state (per-mission artifacts, regenerated)
.newton/

# Runtime attachment store (user files uploaded to a chat)
.pcd-attachments/

# Stray root build artifact (the tracked binary is dist/pcd.exe)
/pcd.exe
